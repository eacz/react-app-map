import { MapView, SearchBar, ToMyLocation } from "../components"

const HomePage = () => {
  return (
    <div>
      <MapView />
      <ToMyLocation />
      <SearchBar />
    </div>
  )
}

export default HomePage
